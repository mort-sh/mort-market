{
  "description": "On Stop: if the allowed git user has finished verified work with a dirty tree, stage everything, AI-commit via /git-commit-staged, and push.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/ship-on-stop.sh\"",
            "timeout": 300
          }
        ]
      }
    ]
  }
}
